
/**
 * I goofed, so this is just an empty migration
 * @type {{up(*, *): Promise<*>, down(*, *): Promise<*>}}
 */
module.exports = {
    async up(db, client) {
    },

    async down(db, client) {
    }
};
